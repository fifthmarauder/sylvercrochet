import { ReactNode } from "react";

export const metadata = {
  title: "Sylver Crochet - About Us",
  description: "Learn more about our journey of crocheting!",
};

export default function ShopLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
