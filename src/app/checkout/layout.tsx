import { ReactNode } from "react";

export const metadata = {
  title: "Sylver Crochet - Checkout",
  description:
    "Add your details and you are one step closer to having your own cute crocheted items!",
};

export default function ShopLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
