import { ReactNode } from "react";

export const metadata = {
  title: "Sylver Crochet - Contact Us",
  description: "Have any crochet related questions? Reach out to us here!!",
};

export default function ShopLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
